/// <summary>
/// Player weapon slots matching original SWIV 3D arsenal.
/// Plasma is always primary (Fire). Specials are selected with SwitchAmmo and fired with AltFire.
/// </summary>
public enum WeaponType {
    Plasma,
    Rockets,
    HomingMissiles,
    Napalm,
    SmartBomb
}
